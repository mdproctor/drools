package org.drools.core;
/** TODO #6650: Temporary stub — EagerPhreakBuilder not needed in vol2 (different eval model). */
public class EagerPhreakBuilder {
    public static boolean isInsideSubnetwork(PathEndNode endNode, SegmentMemory smem) {
        return false; // vol2 stub
    }
}
