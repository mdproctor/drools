package org.drools.core;

/**
 * TODO #6650: Temporary stub class — queries are unified with rules in vol2,
 * so this node type may not be needed. Kept for compilation only.
 */
public class QueryElementNode extends BaseNode {
    public QueryElementNode() {
        super(0, 0, 0);
    }
}
